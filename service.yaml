apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo_task_helm.fullname" . }}
  labels:
    {{- include "demo_task_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo_task_helm.selectorLabels" . | nindent 4 }}
